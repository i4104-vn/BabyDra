[package]
name = "babydra-theme"
version.workspace = true
edition = "2021"
description = "Theme package engine: loads tokens.json + theme.css + fonts.json and resolves theme values."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
