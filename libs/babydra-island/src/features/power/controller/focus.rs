//! Layer shell keyboard interactivity control (re-exported from core island focus controller).

pub use crate::island::controller::focus::{
    acquire_layer_keyboard_focus, release_layer_keyboard_focus,
};
